# Historical review rounds used as regression fixtures

These directories are historical research artifacts, but several tests use selected files as regression fixtures. They remain in place to keep those tests reproducible. New research rounds should document their model, prompt, inputs, reviewer process, and artifact hashes.
