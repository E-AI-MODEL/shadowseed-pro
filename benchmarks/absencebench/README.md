# AbsenceBench lane

This directory documents the repository's AbsenceBench integration path.

Current status: `benchmark_preparation`.

The public dataset, paper, and runner structure have been identified. A live end-to-end route and score mapping still require independent verification. Until that happens, generated records keep `execution_gap=true` and do not claim a benchmark score.
