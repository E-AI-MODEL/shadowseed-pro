# Behavioral evaluation

This layer measures whether promoted seeds improve follow-up, retrieval, probing, warnings, or answers without adding noise. Detector coverage alone is not evidence of downstream usefulness.

This package records evaluation ownership and does not change runtime behavior.
