# Transfer evaluation

This layer measures whether findings survive changes in topic, wording, dataset, model, or backend. It must keep tuning data separate from transfer data and report failed transfers.

No stable dedicated CLI command exists yet. This package records intended ownership without changing runtime behavior.
