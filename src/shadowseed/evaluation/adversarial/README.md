# Adversarial evaluation

This layer tests unsupported promotion, contradiction handling, stale traces, seed spam, prompt injection, and uncontrolled influence. Casebook outputs should make both failures and successful blocks inspectable.

This package records evaluation ownership and does not change runtime behavior.
