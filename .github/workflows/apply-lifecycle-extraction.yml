name: Apply lifecycle extraction

on:
  push:
    branches:
      - refactor/extract-lifecycle
    paths:
      - .github/scripts/apply_lifecycle_extraction.py
      - .github/workflows/apply-lifecycle-extraction.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/extract-lifecycle
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply lifecycle extraction
        run: |
          python .github/scripts/apply_lifecycle_extraction.py
          python -m compileall -q src tests
      - name: Commit lifecycle extraction
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Extract lifecycle workflows"
          git push origin HEAD:refactor/extract-lifecycle
